@model List<satinalma.Models.Comment>
@if(Model!=null)
  {
    @foreach (var item in Model)
    {

        <div class="border p-3 mb-3">
            <p><strong>@item.UserName</strong></p>
            <p>@item.Content</p>
        </div>
    }
  }

